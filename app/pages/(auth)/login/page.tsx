import React from 'react'

const LoginPage = () => {
  return (
    <div>LoginPage - This is the page that will be become the group layout of router Authentication, hence the /auth/ will be omitted from the URL</div>
  )
}

export default LoginPage